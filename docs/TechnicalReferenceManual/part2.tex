% chap2.tex
\part{구조 및 인터페이스 명세}
\label{part:ioref}

% ---------------------------------------------------------------------------- %
%                                  NEW SECTION                                 %
% ---------------------------------------------------------------------------- %

\chapter{디렉토리 구조}
\simulator 는 핵심 알고리즘을 포함하는 python 모듈(\pymodule)과 함께, 독립 실행형 Python 런타임, 실행 스크립트, 기술문서, 및 예시파일을 포함한다.
디렉토리의 각 구성 요소별 내용은 \textcref{table:release-dirstructure}\과 같다.

\begin{defaulttable}
  \caption{\simulator~디렉토리 구성}
  \label{table:release-dirstructure}
  \begin{tabularx}{\textwidth}{l X c}
    \toprule
    \textbf{구성 요소 (경로)} & \textbf{내용} & \textbf{참조} \\
    \midrule
    
    \textbf{src/} & 엔진의 핵심 알고리즘이 포함된 소스 코드 & - \\
    \hspace{0.5em} $\llcorner$ \pymodule/ & \simulator 친화적 구조 및 중간 변환 & \ref{section:howtorun-pythonmodule} \\
    \hspace{0.5em} $\llcorner$ idragon/ & EnergyPlus 친화적 구조 및 idf 변환 & - \\
    \hspace{0.5em} $\llcorner$ launcher/ & Excel 기반 입력파일 실행을 위한 GUI 서버 및 클라이언트 & \ref{section:howtorun-excellauncher} \\
    \addlinespace 
    
    \textbf{docs/} & 엔진 관련 명세 & \ref{section:dirstructure-documentation} \\
    \hspace{0.5em} $\llcorner$ \texttt{TechnicalReferenceManual} & \simulator 의 구조 및 알고리즘 명세 (본 문서) & - \\
    \hspace{0.5em} $\llcorner$ \texttt{RegressionTestReport} & 버전 검증용 테스트 파일 실행 결과 & - \\
    \hspace{0.5em} $\llcorner$ \texttt{ReleaseNote} & 버전 관리 규칙 및 업데이트 내용 & - \\
    \addlinespace

    \textbf{examples/} & 입출력 파일 예시 및 포맷 & - \\
    \hspace{0.5em} $\llcorner$ grm/ & 입력파일 예시 & - \\
    \hspace{0.5em} $\llcorner$ grr/ & 출력파일 예시 & - \\
    \addlinespace

    \textbf{runtime/} & 외부 실행 프로그램 및 데이터 & - \\
    \addlinespace
    \hspace{0.5em} $\llcorner$ PythonV3-12-7/ & 독립실행형 Python 런타임 & \ref{section:dirstructure-devenv} \\
    \hspace{0.5em} $\llcorner$ EnergyPlusV24-2-0/ & 독립실행형 EnergyPlus 엔진 & \ref{section:dirstructure-devenv} \\
    \hspace{0.5em} $\llcorner$ Weather/TMY/ & 시뮬레이션에 필요한 기상 데이터 & \ref{section:weather-data} \\
    
    \texttt{EPlusSimpleCLI.exe} & CLI 기반 엔진 실행 도구 & \ref{section:howtorun-cli} \\
    \texttt{EPlusSimpleLauncher.exe} & Excel 기반 입력파일의 실행을 위한 GUI & \ref{section:howtorun-excellauncher} \\
    
    \bottomrule
  \end{tabularx}
\end{defaulttable}

\section{외부 라이브러리}
\label{section:dirstructure-devenv}

\begin{itemize}
  \item \textbf{Python} \simulator \는 2023년 말 출시되어 현재 가장 널리 쓰이는 버전 중 하나인 \texttt{Python 3.12}\cite{python312}를 사용한다.
  \item \textbf{EnergyPlus} \simulator 에서 EnergyPlus 실행파일 변환 및 실행을 담당하는 idragon 모듈은 2024년 9월 말에 출시된 \texttt{EnergyPlus 24.2}\cite{energyplus242} 버전에 맞추어 개발되었다.
\end{itemize}


\section{기술문서}
\label{section:dirstructure-documentation}

\begin{itemize}
  \item \textbf{Technical Reference Manual} 본 문서.
  \item \textbf{Regression Test Report} Regression Test Report는 소프트웨어의 기능 추가나 코드 수정, 혹은 종속 라이브러리의 업데이트가 기존에 정상적으로 작동하던 기능에 의도치 않은 오류(Regression)를 유발하지 않았음을 검증하고 기록한 문서이다\cite{sommerville2015software}. 이 보고서에서는 표준 테스트용 시뮬레이션을 수행한 결과값, 실행 속도 등을 정량적으로 제시하므로, 이전 버전과 현재 버전 간의 차이를 비교, 엔진의 수치적 정합성과 시스템 안정성을 확인 가능하다.
  \item \textbf{Release Note} \simulator 의 버전별 호환성 등 버전 관리 규칙을 명세하고, 각 버전별 업데이트 내용을 추가, 변경, 수정, 제거 4가지 항목으로 구분하여 기술한다.
\end{itemize}


\chapter{실행 방법}

\simulator\은 아래 세 가지 방법으로 실행할 수 있다.
\begin{itemize}
  \item \textbf{명령줄 인터페이스 (Command Line Interface; CLI)}: EPluSimpleCLI 실행파일을 이용, cmd 콘솔 명령어로 \simulator 의 주요 기능을 실행한다. GUI 프로그램 등, 본 엔진과 연동되는 외부 프로그램에 권장되는 방법이다. 포맷 변환, DB 조회 등 외부 프로그램과의 연동이 필요한 기능들이 지원된다.
  \item \textbf{Excel 기반 입력파일 실행}: Excel 기반 입력파일(\texttt{.xlsx} 확장자)을 작성하여, 내장 GUI (EPlusSimpleLauncher.exe)를 통해 \simulator \를 실행한다. 본 엔진을 독립적으로 사용하는 사용자에게 권장되는 방법이며, 시뮬레이션의 실행만 가능하다.
  \item \textbf{Python 모듈 직접 사용}: 엔진에 포함된 portable Python, 또는 별도의 환경에서 \pymodule\을 직접 import하여 \simulator 의 모든 함수를 활용할 수 있다. 본 엔진을 parametric 시뮬레이션 등 변칙적으로 활용하려는 연구자 및 개발자에게 권장되는 방법이다. 단, EnergyPlus 엔진, 기상데이터와의 상대경로가 유지되거나, 별도로 처리해주어야 한다.
\end{itemize}


\section{명령줄 인터페이스 (CLI)}
\label{section:howtorun-cli}

\simulator\은 명령행 인터페이스(Command-Line Interface, CLI)를 통해 실행할 수 있으며, 배포 패키지에는 이를 위한 실행파일 \inlinecode{EPlusSimpleCLI.exe}가 포함되어 있다. 스크립트는 내부적으로 \simulator 에 포함된 Python을 이용, epsimple 모듈을 실행한다 즉, 아래 CLI signature는 \inlinecode{runtime/PythonV3-12-7/python.exe -m epsimple <command> [args]}와 동일하며, 외부 연동 시 안정성을 위해 Python의 직접 호출보다는 CLI 실행파일을 이용하는 것이 권장된다.

\begin{codeblock}{CLI command signature}
EPlusSimpleCLI <command> [args]
\end{codeblock}

여기서 \inlinecode{<command>}는 시뮬레이션 실행 (\texttt{run}: \ref{subsection:clisubcommands-run}), 포맷 변환(\texttt{convert}: \ref{subsection:clisubcommands-convert}) 두 가지가 지원된다.

\subsection{시뮬레이션 실행}
\label{subsection:clisubcommands-run}

\simulator 의 핵심 기능으로, 입력파일을 받아 시뮬레이션을 실행하고, 결과를 출력파일로 저장하는 기능이다. 입력파일은 \texttt{grm}(json 형식, \ref{section:format-explanation-grm}) 또는 \texttt{xlsx} (\simulator\ 전용 엑셀 입력 형식, \ref{section:format-explanation-xlsx}) 모두 가능하며, 출력파일은 \texttt{grr}(json 형식, \ref{section:format-explanation-grr})이다.

\begin{codeblock}{CLI subcommand signature: `run'}
EPlusSimpleCLI run --input <input_filepath> [--output <output_filepath>]
\end{codeblock}

\paragraph{입력 인자}
\begin{description}
  \item[\texttt{-i}, \texttt{--input}] 입력파일의 경로. \texttt{grm} 또는 \texttt{xlsx} 형식의 파일이 가능하다. 입력파일의 형식은 확장자에 따라 \texttt{xlsx} 확장자는 Excel 기반 입력파일로, 그 외는 json 형식의 입력파일로 인식된다.
  \item[\texttt{-o}, \texttt{--output}] 출력파일의 경로. 지정하지 않으면 입력파일의 경로에서 확장자만 \texttt{grr}로 바꾼 경로에 저장된다.
\end{description}

\subsection{포맷 변환}
\label{subsection:clisubcommands-convert}

입력파일의 형식을 변환하는 기능이다. \simulator 에서 입력파일은 \texttt{xlsx} $\rightarrow$ \texttt{grm} $\rightarrow$ \texttt{idf} 순으로 변환되어 시뮬레이션되므로, \texttt{xlsx} $\rightarrow$ \texttt{idf}를 포함하여 3가지 변환이 가능하다.

\begin{codeblock}{CLI subcommand signature: `convert'}
EPlusSimpleCLI convert <input_filepath> --source <input_type> --destination <output_type> [--output_filepath <output_filepath>]
\end{codeblock}

\paragraph{입력 인자}
\begin{description}
  \item[\texttt{input\_filepath}] 변환할 입력파일의 경로. \texttt{grm} 또는 \texttt{xlsx} 형식의 파일이 가능하다.
  \item[\texttt{-s}, \texttt{--source}] 입력파일의 형식. 실제 형식에 맞게 \texttt{excel}, \texttt{json} 중 하나를 지정할 수 있다.
  \item[\texttt{-d}, \texttt{--destination}] 출력파일의 형식. \texttt{json}(입력파일의 형식이 \texttt{excel}인 경우) 또는 \texttt{idf} 중 하나를 지정할 수 있다.
  \item[\texttt{-o}, \texttt{--output\_filepath}] 출력파일의 경로. 지정하지 않으면 입력파일의 경로에서 확장자만 변경된 경로에 저장된다.
\end{description}

\section{Excel 기반 입력파일의 실행 방법}
\label{section:howtorun-excellauncher}

배치 스크립트 \inlinecode{EPlusSimpleLauncher.exe}를 실행하면, 자체 window가 실행된다 (내부서버 실행에 시간이 소요될 수 있음). Excel 기반 입력파일(\texttt{.xlsx} 확장자)을 업로드하여 \simulator \를 실행할 수 있다. GUI는 입력파일의 형식이 올바른지 검증한 뒤, 시뮬레이션을 실행하고, 결과를 레포트 형식으로 제공한다.

웹 기반 GUI는 최대 10개의 리모델링 이후 모델을 동시에 업로드, 시뮬레이션 및 비교할 수 있으며, 최대 4개 코어를 활용하는 병렬 시뮬레이션을 지원한다.


\section{Python 모듈(\pymodule) 직접 사용법}
\label{section:howtorun-pythonmodule}

\pymodule 의 주요 함수는 \texttt{/src/epsimple/api.py}에 정의되어있으며, \pymodule 을 import하여 사용이 가능하다. 주요 함수 및 입력 인자는 \ref{section:howtorun-cli}에서 기술한 CLI와 유사하다.

\begin{codeblock}{Python module API example}
from epsimple import (
  run_grjson         ,
  run_grexcel        ,
  convert_inputformat,
)

run_grjson(
  "my_input.grm" ,
  "my_output.grr",
)
\end{codeblock}


\chapter{입력 및 출력 파일 명세}

\section{입력파일 (grm)}
\label{section:format-explanation-grm}

\simulator 의 입력파일은 실제 건물의 물리적 구성 관계를 반영하여 계층적으로 구성되어 있다 \parencref{fig:grm_structure}. 즉, 건물(\texttt{building})을 최상위 객체로 하여 층(\texttt{floor}), 실(\texttt{zone}), 면(\texttt{surface}), 개구부(\texttt{fenestration}) 등과 같은 공간 요소가 실제 건물의 포함 관계와 유사한 형태로 단계적으로 조직된다. 이러한 구조를 통해 건물의 공간적 구성과 시스템 연결 관계를 직관적으로 표현할 수 있다.

한편, 재료(\texttt{material})나 구조체(\texttt{construction})과 같은 물리적 속성은 공간 객체 내부에 직접 중복 정의되지 않고 별도의 객체로 정의된 뒤 참조 방식으로 연결된다. 예를 들어 면(\texttt{surface})은 특정 구조체(\texttt{surface\_construction})를 참조하는 방식으로 구성된다. 이러한 구조는 동일한 정보를 여러 객체에서 재사용할 수 있도록 한다. 또한, 설비 시스템(\texttt{hvac\_system})은 건물 객체의 하위에 별도의 분류로 정의되어, 공간 요소와는 독립적으로 구성되며, 필요한 경우 공간 요소와 연결될 수 있도록 설계되어 있다.

\subsection{규칙}

\subsubsection{변수의 명칭 관련}

\paragraph{사용가능한 문자} 모든 변수의 명칭은 영문 소문자 및 밑줄(\_)로만 구성한다.

\paragraph{복수형과 단수형} 복수 객체 \texttt{array}를 담는 변수의 명칭(예: ``floors") 또는 복수 객체를 담는 하위 분류를 사용하는 변수의 명칭(예: ``profile\_components"), 길이는 복수형을 사용하며, 단일 값을 담는 변수의 명칭(예: ``name")은 단수형을 사용한다. 단, 길이가 정해져있거나 각 위치별 의미가 명시적으로 다른 \texttt{array}를 담는 변수의 명칭(예: ``vintage")은 단수형을 사용한다.

\paragraph{축약형의 사용} 모든 변수의 명칭은 축약형을 사용하지 않는 것을 원칙으로 하되, 약어 자체로 통용되는 경우(예: ``cop\_heating")에는 축약형을 사용한다.

\subsubsection{ID 기반 객체 참조 관련} \label{subsubsection:ioref:id_description}
grjson에서 다른 객체의 지칭은 \texttt{ID}값에 기반하여 이루어지며, 모든 객체의 이름(``name")은 사용자 편의를 위한 이름으로 실제 데이터 변환 과정에서 사용되지 않는다.

\paragraph{사용가능한 문자} 모든 ID는 영문 대소문자, 숫자, 하이픈(-), 밑줄(\_)로만 구성되며, 숫자, 하이픈(-) 또는 밑줄(\_)로 시작하지 않는다.

\paragraph{ID의 중복} 동일한 \texttt{ID}는 같은 \texttt{class}의 객체끼리 공유할 수 없다. (모든 \texttt{class}에 대하여 서로 다른 \texttt{ID} 사용을 권장)

\subsubsection{변수의 유효성}

% ------------------------------- 여기서만 쓰는 함수들 ------------------------------- %

% --- 공통 chip (그대로 사용) ---
\newtcbox{\tagchip}[1][]{%
  on line, boxsep=0.6ex, left=0.9ex, right=0.9ex, top=0.2ex, bottom=0.2ex,
  arc=2.2pt, boxrule=0pt, colframe=white,
  colback=black, coltext=white, fontupper=\sffamily\bfseries\footnotesize,
  #1}

% --- 타입 색상 ---
\definecolor{typeString}{RGB}{206,145,120}
\definecolor{typeFloat}{RGB}{181,206,168}
\definecolor{typeInt}{RGB}{78,201,176}
\definecolor{typeEnum}{RGB}{86,156,214}
\definecolor{typeBool}{RGB}{142,206,221}

% --- 요구수준 색상 ---
\definecolor{reqBlueDark}{RGB}{37,99,235}
\definecolor{reqBlueLight}{RGB}{147,197,253}
\definecolor{reqGrayDark}{RGB}{107,114,128}
\definecolor{reqGrayLight}{RGB}{209,213,219}

% --- TypeTag (S/F/I/E) ---
\newcommand{\TypeTag}[1]{%
  \begingroup
  \ifthenelse{\equal{#1}{S}}{\tagchip[colback=typeString,coltext=black]{S}}{%
  \ifthenelse{\equal{#1}{F}}{\tagchip[colback=typeFloat ,coltext=black]{F}}{%
  \ifthenelse{\equal{#1}{I}}{\tagchip[colback=typeInt   ,coltext=white]{I}}{%
  \ifthenelse{\equal{#1}{E}}{\tagchip[colback=typeEnum  ,coltext=white]{E}}{%
  \ifthenelse{\equal{#1}{B}}{\tagchip[colback=typeBool  ,coltext=white]{B}}{%
  \tagchip[colback=gray!60,coltext=black]{#1}}}}}}%
  \endgroup
}

% --- ReqTag (R/CR/O/CO) ---
\newcommand{\ReqTag}[1]{%
  \begingroup
  \ifthenelse{\equal{#1}{R}} {\def\shade{reqBlueDark}\def\fg{white}}{}%
  \ifthenelse{\equal{#1}{CR}}{\def\shade{reqBlueLight}\def\fg{black}}{}%
  \ifthenelse{\equal{#1}{O}} {\def\shade{reqGrayDark}\def\fg{white}}{}%
  \ifthenelse{\equal{#1}{CO}}{\def\shade{reqGrayLight}\def\fg{black}}{}%
  \ifthenelse{\isundefined{\shade}}{\def\shade{gray!60}\def\fg{black}}{}%
  \tagchip[colback=\shade,coltext=\fg]{#1}%
  \endgroup
}

% --- table definition --- 
\newcommand{\jsontableunit}[1]{%
  \ifthenelse{\equal{#1}{}}{-}{$#1$}%
}

\newcommand{\jsontabledash}[1]{%
  \ifthenelse{\equal{#1}{}}{-}{#1}%
}

\newcommand{\jsontablerow}[7]{%
  \nolinkurl{#1} & #2 & #3 &
  \jsontabledash{#4} & \jsontabledash{#5} & \jsontabledash{#6} & \jsontableunit{#7} \\
}

\newcommand{\needtobeexplain}{상세설명 참고}

\newcommand{\jsontable}[2]{
  \begin{table}[htbp]
    \centering
    \caption{grjson 구조에서 \texttt{#1} 클래스의 속성들}
    {\footnotesize
    \begin{tabularx}{\textwidth}{l c c c Y Y c}
      \toprule
      변수명 & 타입 & 필수 & 기본값 & 조건 & 범위/길이/선택지 & 단위 \\
      \midrule
      #2
      \bottomrule
    \end{tabularx}
   } 
  \end{table}
}

모든 변수의 유효성은 필수 및 조건부 사용 여부에 따라 변수(의 속성) 값 존재의 유효성을 검사한 뒤, 타입 및 범위(또는 열거형 상수) 만족 여부를 검사하여 확인할 수 있다.

\paragraph{타입} 모든 변수는 정해진 타입의 변수여야 한다. 모든 변수의 타입은 문자\TypeTag{S}($String$), 실수\TypeTag{F}($Float$), 정수\TypeTag{I}($Integer$), 열거형\TypeTag{E}($Enum$), 불리언\TypeTag{B} 로 구분할 수 있다. 

\paragraph{필수 여부} 입력되지 않을 경우 에러가 발생하는 필수 변수\ReqTag{R}($Required$)와 달리, 비 필수 변수\ReqTag{O}($Optional$)는 미 입력 시 기본값이 사용된다. 변수 자체를 생략할 수 있고, 입력하지 않았다는 것을 명시적으로 \texttt{null}로 표시할 수도 있다.

\paragraph{조건부 사용 여부} 어떤 변수는 다른 변수(주로 \texttt{type})의 값에 따라 조건부로 사용된다. 이 경우에도 해당 조건에서 필수적으로 요구되는 변수\ReqTag{CR}($Conditional Required$)와 그렇지 않은 변수\ReqTag{CO}($Conditional Optional$)로 구분할 수 있다. 조건에 만족하지 않는 경우, 해당 변수는 json에서 생략될 수 있다.

\paragraph{범위 및 열거형 상수} array면 길이 제약 따짐.
실수\TypeTag{F} 및 정수\TypeTag{I} 타입의 변수는 Python 부동소수점 및 정수 형식에서 표현 가능한 범위 내에서 처리된다.

\subsubsection{기타 규칙}

\paragraph{단위} 모든 변수는 SI 기본단위를 사용한다 (예: $m$, $J$, $W$, $kg$).

\subsection{class별 명세}

\subsubsection{building (건물정보)} \label{subsubsection:ioref:building}
실재하는 건물에 대한 최상위 정보 체계이다.

\jsontable{building}{
  \jsontablerow{name                  }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{north_axis            }{\TypeTag{F}}{\ReqTag{R}}{}{}{[0,360)}{deg(^\circ)}
  \jsontablerow{address               }{\TypeTag{S}}{\ReqTag{R}}{}{}{\needtobeexplain}{}
  \jsontablerow{vintage               }{[ \TypeTag{I} ]}{\ReqTag{R}}{}{}{[3,3]}{}
  \jsontablerow{is_multifamily_housing}{\TypeTag{B}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{floors                }{[ \hyperref[subsubsection:ioref:floor]{\texttt{floor}} ]}{}{}{}{[1, $Inf$)}{}
  \jsontablerow{supply_systems        }{\hyperref[subsubsection:ioref:supplysystems]{\texttt{supply\_systems}}}{\ReqTag{R}}{}{}{}{}
\jsontablerow{source_systems          }{\hyperref[subsubsection:ioref:sourcesystems]{\texttt{source\_systems}}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{ventilation_systems   }{[ \hyperref[subsubsection:ioref:ventilationsystem]{\texttt{ventilation\_system}} ]}{\ReqTag{R}}{}{}{[0, $Inf$)}{}
  \jsontablerow{photovoltaic_systems  }{[ \hyperref[subsubsection:ioref:photovoltaicsystem]{\texttt{photovoltaic\_system}} ]}{\ReqTag{R}}{}{}{[0, $Inf$)}{}
}

\paragraph{name (건물명)} 건물의 이름. 시뮬레이션에 직접 사용되지 않는다.
\paragraph{north\_axis (방위각)} 건물의 방위각. 도면상에서 나침반이 가리키는 방향 (반시계방향), 또는 건물의 향 관련 입력을 모두 회전시키는 동작 (시계방향) 을 지칭한다 (\cref{fig:ioref:north_axis}).

\begin{defaultfigure}
  
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[scale=0.6]{north_axis 설명도식 (도면에서).png}
    \caption{도면에서}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[scale=0.6]{north_axis 설명도식 (지도에서).png}
    \caption{지도에서}
  \end{subfigure}
  
  \caption{north\_axis의 의미와 surface azimuth와의 관계}
  \label{fig:ioref:north_axis}

\end{defaultfigure}

\paragraph{address (주소)} 건물의 주소. 식별 가능한 법정 기초행정구역명(세종특별자치시와 제주특별자치도 제주시, 서귀포시를 포함)을 포함하여야 한다. 

\paragraph{vintage (허가일자)} 건물의 허가일자. 연(4자리 정수), 월(2자리 정수), 일(2자리 정수)로 이루어진 array이다.

\paragraph{is\_multifamily\_housing (공동주택 여부)} 건축법 시행령 및 건축물의 에너지절약설계기준 상 공동주택에 적용되는 법규를 적용받는지 여부.

\subsubsection{floor (층)} \label{subsubsection:ioref:floor}
하위 \hyperref[subsubsection:ioref:zone]{\texttt{zone}}들을 층 단위로 구분하기 위한 클래스이다.

\paragraph{supply\_systems (공급설비 목록)}
건물에 정의된 공급설비를 종류별로 구분하여 저장하는 객체이다.
각 하위 key의 값은 해당 종류의 공급설비 객체로 구성된 배열이다.
공급설비가 하나도 없는 경우 빈 객체 \texttt{\{\}}를 입력한다.

\paragraph{source\_systems (생산설비 목록)}
건물에 정의된 생산설비를 종류별로 구분하여 저장하는 객체이다.
각 하위 key의 값은 해당 종류의 생산설비 객체로 구성된 배열이다.
생산설비가 하나도 없는 경우 빈 객체 \texttt{\{\}}를 입력한다.

\jsontable{floor}{
  \jsontablerow{floor_number}{\TypeTag{I}}{\ReqTag{O}}{null}{}{}{}
  \jsontablerow{zones}{[ \hyperref[subsubsection:ioref:zone]{\texttt{zone}} ]}{\ReqTag{R}}{}{}{[1,$Inf$)}{}
}

\paragraph{floor\_number (층수)} 층을 구별하기 위한 식별자. 시뮬레이션에 직접 사용되지 않는다.

\subsubsection{zone (실)} \label{subsubsection:ioref:zone}
건물에서 하나의 실, 또는 같은 열적 특성(유사한 용도, 공통 설비 등)을 공유하는 서로 연결된 여러 실을 모사하는 최소 단위이다.

\jsontable{zone}{
  \jsontablerow{id                      }{\TypeTag{S}}{ \ReqTag{R} }{}{}{}{}
  \jsontablerow{name                    }{\TypeTag{S}}{ \ReqTag{R} }{}{}{}{}
  \jsontablerow{height                  }{\TypeTag{F}}{ \ReqTag{R} }{}{}{(0,$Inf$)}{m}
  \jsontablerow{profile                 }{\TypeTag{S}}{ \ReqTag{R} }{}{}{\needtobeexplain}{}
  \jsontablerow{light_density           }{\TypeTag{F}}{ \ReqTag{R} }{}{}{[0,$Inf$)}{W/m^{2}}
  \jsontablerow{infiltration            }{\TypeTag{F}}{ \ReqTag{R} }{}{}{[0,$Inf$)}{ACH50}
  \jsontablerow{supply_system_heating_id}{\TypeTag{S}}{ \ReqTag{R} }{}{}{\hyperref[subsubsection:ioref:supplysystem]{\texttt{supply\_system}}의 \texttt{id}}{}
  \jsontablerow{supply_system_cooling_id}{\TypeTag{S}}{ \ReqTag{R} }{}{}{\hyperref[subsubsection:ioref:supplysystem]{\texttt{supply\_system}}의 \texttt{id}}{}
  \jsontablerow{ventilation_systems     }{[ \hyperref[subsubsection:ioref:ventilationassignment]{\texttt{ventilation\_assignment}} ]}{\ReqTag{R}}{}{}{[0, $Inf$)}{}

  \jsontablerow{surfaces                }{[ \hyperref[subsubsection:ioref:surface]{\texttt{surface}} ]}{ \ReqTag{R} }{}{}{[0, $Inf$)}{}
}

\paragraph{id (식별자)} 존의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 존 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{height (천장고)} 존의 높이. 실의 부피 계산에 이용된다.

\paragraph{profile (용도프로필)} 존의 용도프로필. 기본으로 제공되는 항목 중에서 입력 가능하다. 

\paragraph{profile\_id (용도프로필 식별자)} 존에 적용된 custom 용도프로필의 ID. 

\paragraph{light\_density (조명밀도)} 조명 밀도.

\paragraph{supply\_system\_heating\_id (난방 공급 설비)} 난방공급시스템의 ID. 

\paragraph{supply\_system\_cooling\_id (냉방 공급 설비)} 냉방공급시스템의 ID.

\paragraph{ventilation\_systems (환기 설비)} 환기시스템의 ID 및 개수 목록.

\paragraph{surface (소속 면)} 소속면. 해당 \texttt{zone}을 인접존으로 하는 \texttt{surface}를 포함하여, 1개 이상의 천장(\texttt{type} = ``ceiling''), 1개 이상의 바닥(\texttt{type} = ``floor'') 및 1개 이상의 벽체(\texttt{type} = ``wall'')를 필요로 한다. 즉, 인접존이 없을 경우, 3 이상의 길이를 가지는 list가 필요하다.

\subsubsection{ventilation\_system\_assignment (환기설비 배치)}
\label{subsubsection:ioref:ventilationassignment}

각 존에 적용되는 환기설비의 종류와 설치 대수를 정의한다.
하나의 객체는 기존에 정의된 \hyperref[subsubsection:ioref:ventilationsystem]{\texttt{ventilation\_system}} 하나를 참조한다.

\jsontable{ventilation\_assignment}{
  \jsontablerow{id   }{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:ventilationsystem]{\texttt{ventilation\_system}}의 \texttt{id}}{}
  \jsontablerow{count}{\TypeTag{I}}{\ReqTag{R}}{}{}{[1, $Inf$)}{}
}

\paragraph{id (환기설비 식별자)}
해당 존에 적용되는 \hyperref[subsubsection:ioref:ventilationsystem]{\texttt{ventilation\_system}}의 식별자이다.

\paragraph{count (설치 대수)}
해당 환기설비가 존에 적용되는 대수이다.
1 이상의 정수로 입력한다.

\subsubsection{surface (면)} \label{subsubsection:ioref:surface}
존과 존, 외부 (외기 또는 지면), 또는 단열선 사이의 면(벽체)이다.

\jsontable{surface}{
  \jsontablerow{id                  }{\TypeTag{S}}{\ReqTag{R} }{}{}{}{}
  \jsontablerow{name                }{\TypeTag{S}}{\ReqTag{R} }{}{}{}{}
  \jsontablerow{type                }{\TypeTag{E}}{\ReqTag{R} }{}{}{\shortstack[c]{floor \\ wall \\ ceiling}}{}
  \jsontablerow{boundary_condition  }{\TypeTag{E}}{\ReqTag{R} }{}{}{\shortstack[c]{outdoors \\zone \\ ground \\ adiabatic}}{}
  \jsontablerow{area                }{\TypeTag{F}}{\ReqTag{R} }{}{}{(0,$Inf$)}{m^2}
  \jsontablerow{azimuth             }{\TypeTag{F}}{\ReqTag{CR} }{}{\texttt{type} = ``wall'' \newline AND \newline \texttt{boundary\_condition} = ``outdoors''}{[0,360)}{deg(^\circ)}
  \jsontablerow{adjacent_zone_id    }{\TypeTag{S}}{\ReqTag{CR}}{}{\texttt{boundary\_condition} = ``zone''}{\hyperref[subsubsection:ioref:zone]{\texttt{zone}}의 \texttt{id}}{}
  \jsontablerow{construction_id     }{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:construction]{\texttt{construction}}의 \texttt{id} \newline ``open'' \newline ``unknown''}{}
  \jsontablerow{coolroof_reflectance}{\TypeTag{F}}{\ReqTag{CO}}{}{\texttt{type} = ``ceiling'' \newline AND \newline \texttt{boundary\_condition} = ``outdoors''}{(0,1]}{1}
  \jsontablerow{fenestrations       }{[ \hyperref[subsubsection:ioref:fenestration]{\texttt{fenestration}} ]}{\ReqTag{R}}{}{}{[0, $Inf$)}{}
}

\paragraph{id (식별자)} 면의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 면 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{type (유형)} 면의 유형. 천장(``ceiling''), 바닥(``floor''), 벽체(``wall'')로 구분된다. 유형이 바닥(``floor'')인 면의 면적은 해당 \texttt{zone}의 면적에 포함되며, 유형이 천장(``ceiling'')인 면은 경계조건이 외기(``outdoors'')인 경우 쿨루프 적용이 가능하다.

\paragraph{boundary\_condition (경계조건)} 면의 경계조건. 일사와 바람의 영향을 받고 외기와 열교환을 하는 외기(``outdoors''), 다른 \texttt{zone}과 열교환을 하는 존(``zone''), 지면과 열교환을 하는 지면(``ground''), 어떠한 열교환도 하지 않고 축열의 역할만 담당하는 단열(``adiabatic'')로 구분된다 (Figure \ref{fig:surface_bo_description}).

\begin{defaultfigure}
  \includegraphics[width=0.8\textwidth]{면 경계조건 예시.png}
  \caption{면의 경계조건 예시}
  \label{fig:surface_bo_description}
\end{defaultfigure}

\paragraph{area (면적)} 면적. 해당 면에 개구부(예:창, 문 등)가 존재하는 경우, 이들의 면적을 포함한 전체 면적을 의미한다.

\paragraph{azimuth (향)} 면의 방위각. 기준은 북향을 0$^\circ$로 하며, 반시계 방향으로 증가한다. 예를들어, 서향은 90$^\circ$, 남향은 180$^\circ$, 동쪽은 270$^\circ$로 정의한다.

\paragraph{adjacent\_zone\_id (인접존 식별자)} 인접존 ID. 면의 경계조건을 ``zone''으로 설정한 경우에만 입력한다.

\paragraph{construction\_id (구조체 식별자)} 구조체 ID. 

\paragraph{coolroof\_reflectance (쿨루프 반사율)} 지붕면의 태양 반사율. 쿨루프(Cool Roof)시공이 적용된 경우에만 입력한다.

\subsubsection{fenestration (개구부)} \label{subsubsection:ioref:fenestration}
개구부. 벽체의 열린 부분을 의미하며, 창, 문(유리문, 방화문 등) 등 외기와의 접촉 또는 채광·환기를 목적으로 설치된 요소를 포함한다.

\jsontable{fenestration}{
  \jsontablerow{id             }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name           }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{type           }{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{window \\ door \\ glassdoor}}{}
  \jsontablerow{area           }{\TypeTag{F}}{\ReqTag{R}}{}{}{(0,$Inf$)}{m^2}
  \jsontablerow{construction_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:fenestrationconstruction]{\texttt{fenestration\_construction}}의 \texttt{id}}{}
  \jsontablerow{blind          }{\TypeTag{E}}{\ReqTag{O}}{\texttt{null}}{}{\shortstack[c]{ venetian \\ shade}}{}
}

\paragraph{id (식별자)} 개구부의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 개구부 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{type (유형)} 개구부의 유형. 유리창(``window''), 유리문(``glassdoor''), 또는 유리가 아닌 일반 문(``door'', 예: 방화문, 나무문 등)으로 구분된다.

\paragraph{area (면적)} 개구부의 면적. 해당 개구부가 속한 면의 면적보다 작아야 한다.

\paragraph{construction\_id (구조체 식별자)} 개구부의 구조체. \texttt{type}이 ``window'' 또는 ``glassdoor''인 경우, \texttt{is\_transparent} 속성이 \texttt{True}인 구조체여야 한다. \texttt{type}이 ``door''인 경우, \texttt{is\_transparent} 속성이 \texttt{False}인 구조체여야 한다

\paragraph{blind (블라인드)} 블라인드의 종류. 값이 \texttt{null}이면 블라인드가 없는 것을 의미한다.

\subsubsection{supply\_systems (공급설비)}
\label{subsubsection:ioref:supplysystems}

건물에 정의된 공급설비를 종류별로 구분하는 객체이다.
각 속성은 해당 종류의 공급설비 객체 배열이며, 해당 종류의 설비가 없는 경우 속성 자체를 생략한다.
따라서 개별 공급설비 객체는 별도의 \texttt{type} 속성을 가지지 않으며, 소속된 배열의 속성명에 따라 class가 결정된다.

\jsontable{supply\_systems}{
  \jsontablerow{packaged_air_conditioner}{[ \hyperref[subsubsection:ioref:packagedairconditioner]
       {\texttt{packaged\_air\_conditioner}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{air_handling_unit       }{[ \hyperref[subsubsection:ioref:airhandlingunit]
       {\texttt{air\_handling\_unit}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{fan_coil_unit           }{[ \hyperref[subsubsection:ioref:fancoilunit]
       {\texttt{fan\_coil\_unit}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{radiator                }{[ \hyperref[subsubsection:ioref:radiator]
       {\texttt{radiator}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{electric_radiator       }{[ \hyperref[subsubsection:ioref:electricradiator]
       {\texttt{electric\_radiator}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{radiant_floor           }{[ \hyperref[subsubsection:ioref:radiantfloor]
       {\texttt{radiant\_floor}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{electric_radiant_floor  }{[ \hyperref[subsubsection:ioref:electricradiantfloor]
       {\texttt{electric\_radiant\_floor}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
}

\subsubsection{packaged\_air\_conditioner (패키지 에어컨)} \label{subsubsection:ioref:packagedairconditioner}

별도의 생산설비를 참조하지 않고 냉방을 공급하는 독립형 설비이다.

\jsontable{packaged\_air\_conditioner}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{cop_cooling}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
  \jsontablerow{capacity_cooling}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
}

\paragraph{cop\_cooling (냉방 COP)} 정격 냉방 성능계수이다. 속성을 생략하거나 \texttt{null}을 입력하면 3.0을 적용한다.

\paragraph{capacity\_cooling (냉방용량)} 설비의 정격 냉방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\subsubsection{air\_handling\_unit (공조기)} \label{subsubsection:ioref:airhandlingunit}

히트펌프 또는 지열히트펌프에서 생산된 냉열 또는 온열을
존에 공급하는 설비이다.

\jsontable{air\_handling\_unit}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{source_system_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:sourcesystems]{\texttt{source\_systems}}에 정의된 생산설비의 \texttt{id}}{}
}

\paragraph{source\_system\_id (생산설비 식별자)} 공조기에 연결되는 생산설비의 식별자이다. \texttt{source\_systems.heatpump} 또는 \texttt{source\_systems.geothermal\_heatpump}에 정의된 객체를
참조해야 한다.

\subsubsection{fan\_coil\_unit (팬코일유닛)} \label{subsubsection:ioref:fancoilunit}

연결된 생산설비에서 공급되는 온수 또는 냉수를 이용하여 존에 난방 또는 냉방을 공급하는 설비이다.

\jsontable{fan\_coil\_unit}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{source_system_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:sourcesystems]{\texttt{source\_systems}}에 정의된 생산설비의 \texttt{id}}{}
}

\paragraph{source\_system\_id (생산설비 식별자)} 팬코일유닛에 연결되는 생산설비의 식별자이다. 난방용 팬코일유닛은 \texttt{boiler} 또는 \texttt{district\_heating}을 참조하며, 냉방용 팬코일유닛은 \texttt{chiller} 또는 \texttt{absorption\_chiller}를 참조한다.

\subsubsection{radiator (방열기)} \label{subsubsection:ioref:radiator}

보일러 또는 지역난방에서 공급되는 온수를 이용하여 존에 난방을 공급하는 방열기이다.

\jsontable{radiator}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{capacity_heating}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{source_system_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:sourcesystems]{\texttt{source\_systems}}에 정의된 생산설비의 \texttt{id}}{}
}

\paragraph{capacity\_heating (난방용량)} 방열기의 정격 난방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{source\_system\_id (생산설비 식별자)} 방열기에 온수를 공급하는 생산설비의 식별자이다. \texttt{boiler} 또는 \texttt{district\_heating} 객체를 참조해야 한다.

\subsubsection{electric\_radiator (전기 방열기)} \label{subsubsection:ioref:electricradiator}

별도의 생산설비를 참조하지 않고 전기를 이용하여 존에 난방을 공급하는 방열기이다.

\jsontable{electric\_radiator}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{capacity_heating}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
}

\paragraph{capacity\_heating (난방용량)} 전기 방열기의 정격 난방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\subsubsection{radiant\_floor (바닥난방)} \label{subsubsection:ioref:radiantfloor}

보일러 또는 지역난방에서 공급되는 온수를 이용하는 온수식 바닥복사난방 설비이다.

\jsontable{radiant\_floor}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{source_system_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:sourcesystems]{\texttt{source\_systems}}에 정의된 생산설비의 \texttt{id}}{}
}

\paragraph{source\_system\_id (생산설비 식별자)} 바닥닥난방에 온수를 공급하는 생산설비의 식별자이다.\texttt{boiler} 또는 \texttt{district\_heating} 객체를 참조해야 한다.

\subsubsection{electric\_radiant\_floor (전기 바닥난방)} \label{subsubsection:ioref:electricradiantfloor}

별도의 생산설비를 참조하지 않고 전기를 이용하는 바닥복사난방 설비이다.

\jsontable{electric\_radiant\_floor}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
}

\subsubsection{source\_systems (생산설비)} \label{subsubsection:ioref:sourcesystems}

건물에 정의된 생산설비를 종류별로 구분하는 객체이다. 각 속성은 해당 종류의 생산설비 객체 배열이며, 해당 종류의 설비가 없는 경우 속성 자체를 생략한다. 따라서 개별 생산설비 객체는 별도의 \texttt{type} 속성을 가지지 않으며, 소속된 배열의 속성명에 따라 class가 결정된다.

\jsontable{source\_systems}{
  \jsontablerow{heatpump}{[ \hyperref[subsubsection:ioref:heatpump]{\texttt{heatpump}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{geothermal_heatpump}{[ \hyperref[subsubsection:ioref:geothermalheatpump]{\texttt{geothermal\_heatpump}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{chiller}{[ \hyperref[subsubsection:ioref:chiller]{\texttt{chiller}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{absorption_chiller}{[ \hyperref[subsubsection:ioref:absorptionchiller]{\texttt{absorption\_chiller}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{boiler}{[ \hyperref[subsubsection:ioref:boiler]{\texttt{boiler}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
  \jsontablerow{district_heating}{[ \hyperref[subsubsection:ioref:districtheating]{\texttt{district\_heating}} ]}{\ReqTag{O}}{}{}{[1,$Inf$)}{}
}


\subsubsection{heatpump (히트펌프)} \label{subsubsection:ioref:heatpump}

냉방과 난방을 공급할 수 있는 히트펌프 생산설비이다.

\jsontable{heatpump}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{fuel_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{electricity \\ natural\_gas \\ lpg \\ oil \\ district\_heating}}{}
  \jsontablerow{capacity_cooling}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{capacity_heating}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{cop_cooling}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
  \jsontablerow{cop_heating}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
}

\paragraph{fuel\_type (연료 종류)} 히트펌프의 에너지원을 정의한다.

\paragraph{capacity\_cooling (냉방용량)} 정격 냉방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{capacity\_heating (난방용량)} 정격 난방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{cop\_cooling (냉방 COP)} 정격 냉방 성능계수이다.

\paragraph{cop\_heating (난방 COP)} 정격 난방 성능계수이다.

\subsubsection{geothermal\_heatpump (지열히트펌프)} \label{subsubsection:ioref:geothermalheatpump}

지열을 열원으로 사용하는 히트펌프 생산설비이다.

\jsontable{geothermal\_heatpump}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{fuel_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ electricity \\ natural\_gas \\ lpg \\ oil \\ district\_heating}}{}
  \jsontablerow{capacity_cooling}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{capacity_heating}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{cop_cooling}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
  \jsontablerow{cop_heating}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
}

\paragraph{fuel\_type (연료 종류)} 지열히트펌프의 에너지원을 정의한다.

\paragraph{capacity\_cooling (냉방용량)} 정격 냉방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{capacity\_heating (난방용량)} 정격 난방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{cop\_cooling (냉방 COP)} 정격 냉방 성능계수이다. 

\paragraph{cop\_heating (난방 COP)} 정격 난방 성능계수이다.

\subsubsection{chiller (냉동기)} \label{subsubsection:ioref:chiller}

전기를 이용하여 냉수를 생산하는 냉동기이다.
냉각탑의 종류와 제어방식을 함께 정의한다.

\jsontable{chiller}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{compressor_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ turbo \\ screw \\  reciprocating }}{}
  \jsontablerow{coolingtower_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ open \\ closed}}{}
  \jsontablerow{coolingtower_control}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ single-speed \\ two-speed}}{}
  \jsontablerow{capacity_cooling}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{cop_cooling}{\TypeTag{F}}{\ReqTag{O}}{3.0}{}{(0,$Inf$)}{W/W}
  \jsontablerow{coolingtower_capacity}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
}

\paragraph{compressor\_type (압축기 종류)} 냉동기에 적용되는 압축기의 종류이다.

\paragraph{coolingtower\_type (냉각탑 종류)} 냉동기의 응축열을 방출하는 냉각탑의 형식이다.

\paragraph{coolingtower\_control (냉각탑 제어방식)} 냉각탑의 속도 제어방식이다.

\paragraph{capacity\_cooling (냉방용량)} 냉동기의 정격 냉방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{cop\_cooling (냉방 COP)} 냉동기의 정격 냉방 성능계수이다. 

\paragraph{coolingtower\_capacity (냉각탑 용량)} 냉각탑의 정격 열제거용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\subsubsection{absorption\_chiller (흡수식 냉동기)} \label{subsubsection:ioref:absorptionchiller}

연동 보일러에서 공급되는 열을 이용하여 냉수를 생산하는 흡수식 냉동기이다.

\jsontable{absorption\_chiller}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{fuel_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ electricity \\ natural\_gas \\ lpg \\ oil \\ district\_heating}}{}
  \jsontablerow{capacity_cooling}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{cop_cooling}{\TypeTag{F}}{\ReqTag{O}}{0.9}{}{(0,$Inf$)}{W/W}
  \jsontablerow{boiler_efficiency}{\TypeTag{F}}{\ReqTag{O}}{0.85}{}{(0,1]}{1}
}

\paragraph{fuel\_type (연동 보일러 연료)} 흡수식 냉동기에 열을 공급하는 연동 보일러의 연료 종류이다.

\paragraph{capacity\_cooling (냉방용량)} 흡수식 냉동기의 정격 냉방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{cop\_cooling (냉방 COP)} 흡수식 냉동기의 정격 냉방 성능계수이다.

\paragraph{boiler\_efficiency (연동 보일러 효율)} 흡수식 냉동기에 열을 공급하는 연동 보일러의 효율이다.

\subsubsection{boiler (보일러)} \label{subsubsection:ioref:boiler}

연료를 소비하여 난방용 온수를 생산하는 보일러이다. 
\texttt{hotwater\_supply}가 \texttt{true}이면 급탕 생산설비로도 사용된다.

\jsontable{boiler}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{fuel_type}{\TypeTag{E}}{\ReqTag{R}}{}{}{\shortstack[c]{ electricity \\ natural\_gas \\ lpg \\ oil \\ district\_heating}}{}
  \jsontablerow{hotwater_supply}{\TypeTag{B}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{capacity_heating}{\TypeTag{F}}{\ReqTag{O}}{\texttt{null}}{}{(0,$Inf$)}{W}
  \jsontablerow{efficiency}{\TypeTag{F}}{\ReqTag{O}}{0.85}{}{(0,1]}{1}
}

\paragraph{fuel\_type (연료 종류)} 보일러에서 사용하는 연료의 종류이다.

\paragraph{hotwater\_supply (급탕 공급 여부)} 보일러를 급탕 생산설비로 사용할지를 나타낸다. \texttt{true}이면 해당 보일러가 급탕 부하를 분담한다.

\paragraph{capacity\_heating (난방용량)} 보일러의 정격 난방용량이다. 속성을 생략하거나 \texttt{null}을 입력하면 EnergyPlus의 autosizing을 이용한다.

\paragraph{efficiency (효율)} 보일러의 연료 투입량 대비 유효 열생산량의 비율이다.

\subsubsection{district\_heating (지역난방)} \label{subsubsection:ioref:districtheating}

외부 지역난방망에서 공급되는 열을 건물의 난방 또는 급탕에 사용하는 생산설비이다.

\jsontable{district\_heating}{
  \jsontablerow{id}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name}{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{hotwater_supply}{\TypeTag{B}}{\ReqTag{R}}{}{}{}{}
}

\paragraph{hotwater\_supply (급탕 공급 여부)} 지역난방을 급탕 열원으로 사용할지를 나타낸다. \texttt{true}이면 해당 지역난방 설비가 급탕 부하를 분담한다.

\subsubsection{ventilation\_system (환기설비)} \label{subsubsection:ioref:ventilationsystem}

\jsontable{ventilation\_system}{
  \jsontablerow{id             }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name           }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{airflow_rate   }{\TypeTag{F}}{\ReqTag{R}}{}{}{(0,$Inf$)}{m^3/s}
  \jsontablerow{efficiency_heating}{\TypeTag{F}}{\ReqTag{O}}{0.70}{}{(0,1)}{1}
  \jsontablerow{efficiency_cooling}{\TypeTag{F}}{\ReqTag{O}}{0.45}{}{(0,1)}{1}
}

\paragraph{id (식별자)} 환기설비의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 생산설비 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{airflow\_rate (풍량)} 환기설비의 풍량.

\paragraph{efficiency\_cooling (냉방효율)} 유효 냉방 전열교환 효율.

\paragraph{efficiency\_heating (난방효율)} 유효 난방 전열교환 효율.

\subsubsection{photovoltaic\_system (PV패널)} \label{subsubsection:ioref:photovoltaicsystem}
태양광 발전 설비.

\jsontable{photovoltaic\_system}{
  \jsontablerow{id        }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name      }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{area      }{\TypeTag{F}}{\ReqTag{R}}{}{}{(0,$Inf$)}{m^2}
  \jsontablerow{azimuth   }{\TypeTag{F}}{\ReqTag{R}}{}{}{[0,360)}{deg(^\circ)}
  \jsontablerow{tilt      }{\TypeTag{F}}{\ReqTag{R}}{}{}{[0,90]}{deg(^\circ)}
  \jsontablerow{efficiency}{\TypeTag{F}}{\ReqTag{R}}{}{}{(0,1)}{1}
}

\paragraph{id (식별자)} 태양광설비의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.
 
\paragraph{name (이름)} 사용자 편의를 위해 제공되는 태양광설비 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{area (면적)} 태양광 패널의 면적.

\paragraph{efficiency (효율)} 태양광 모듈의 발전 효율.

\paragraph{azimuth (방위각)} 패널의 방위각. \hyperref[subsubsection:ioref:building]{\texttt{building}}의 \texttt{north\_axis}를 고려하지 않고 독립적으로 계산된다 (Figure \ref{fig:azimuth_of_PVpanel}). 

\begin{defaultfigure}
  \includegraphics{태양광패널의 azimuth 설명.png}
  \caption{태양광 패널의 \texttt{azimuth}}
  \label{fig:azimuth_of_PVpanel}
\end{defaultfigure}

\paragraph{tilt (경사각)} 패널의 경사각. 0$^\circ$는 수평면, 90$^\circ$는 수직면을 의미한다.

\subsubsection{material (재료)} \label{subsubsection:ioref:material}

\jsontable{material}{
  \jsontablerow{id           }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name         }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{conductivity }{\TypeTag{F}}{\ReqTag{R}}{}{}{}{W{\cdot}m^{-1}{\cdot}K^{-1}}
  \jsontablerow{density      }{\TypeTag{F}}{\ReqTag{R}}{}{}{}{kg{\cdot}m^{-3}}
  \jsontablerow{specific_heat}{\TypeTag{F}}{\ReqTag{R}}{}{}{(100,$Inf$)}{J{\cdot}kg^{-1}{\cdot}K^{-1}}
}

\paragraph{id (식별자)} 재료의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 재료 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{conductivity (열전도율)} 열전도율. 재료의 단위 두께를 통한 열 전달 능력을 나타낸다.

\paragraph{density (밀도)} 밀도. 재료의 단위 부피당 질량을 나타낸다.

\paragraph{specific\_heat (비열)} 비열. 재료의 단위 질량이 1K 상승할 때 필요한 열량을 나타낸다.

\subsubsection{surface\_construction (구조체 면)} \label{subsubsection:ioref:construction}

\jsontable{surface\_construction}{
  \jsontablerow{id    }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name  }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{layers}{[ \hyperref[subsubsection:ioref:layer]{\texttt{layer}} ]}{\ReqTag{R}}{}{}{[1, 10]}{}
}
 
\paragraph{id (식별자)} 레이어의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 레이어 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\subsubsection{layer (레이어)} \label{subsubsection:ioref:layer}

 \jsontable{layer}{
  \jsontablerow{material_id}{\TypeTag{S}}{\ReqTag{R}}{}{}{\hyperref[subsubsection:ioref:material]{\texttt{material}}의 \texttt{id}}{}
  \jsontablerow{thickness  }{\TypeTag{F}}{\ReqTag{R}}{}{}{(0.003,$Inf$)}{m}
}

\paragraph{material\_id (재료 식별자)} 레이어 구성 재료의 ID.

\paragraph{thickness (레이어 두께)} 레이어 구성 재료의 두께.

\subsubsection{fenestration\_construction (구조체 개구부)} \label{subsubsection:ioref:fenestrationconstruction}

\jsontable{fenestration\_construction}{
  \jsontablerow{id            }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{name          }{\TypeTag{S}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{is_transparent}{\TypeTag{B}}{\ReqTag{R}}{}{}{}{}
  \jsontablerow{u             }{\TypeTag{F}}{\ReqTag{R}}{}{}{(0, $Inf$)}{W{\cdot}m^{-1}{\cdot}K^{-1}}
  \jsontablerow{g             }{\TypeTag{F}}{\ReqTag{CR}}{}{\texttt{is\_transparent} = \texttt{True}}{(0,1]}{}
}

\paragraph{id (식별자)} 창호의 식별자. \ref{subsubsection:ioref:id_description}에 따라 정의된다.

\paragraph{name (이름)} 사용자 편의를 위해 제공되는 창호 구별 식별자. 중복, 사용가능한 문자에 제한이 없으며, 시뮬레이션에 직접 사용되지 않는다.

\paragraph{is\_transparent (투명여부)} 구조체의 투명여부. 투명한 구조체의 개구부(예: 유리창, 유리문 등)는 ``True''로 입력하며, 불투명한 구조체의 개구부(예: 방화문, 나무문 등)는 ``False''로 입력한다.

\paragraph{u (열관류율)} 창호의 열관류율(U-value).

\paragraph{g (태양열취득계수)} 창호의 열취득계수(G-value, SHGC).

% ---------------------------------------------------------------------------- %
%                                  NEW SECTION                                 %
% ---------------------------------------------------------------------------- %

\section{출력파일 (grr)}
\label{section:format-explanation-grr}

GRR 파일은 시뮬레이션 수행 결과를 저장하기 위한 데이터 구조로, 건물의 에너지 사용량 및 이를 기반으로 계산된 다양한 성능 지표를 포함한다. GRM 파일이 건물 모델과 시뮬레이션 입력 정보를 정의하는 역할을 하는 반면, GRR 파일은 시뮬레이션 결과 데이터를 체계적으로 저장하고 후속 분석이나 보고서 생성에 활용하기 위한 목적으로 생성된다. 데이터 형식은 JSON 기반 구조를 따르며, 건물 기본 정보, 계산에 사용되는 변환계수, 용도별 에너지 사용량, 그리고 집계된 성능 지표로 구성된다.

GRR 데이터의 최상위 구조는 다음과 같은 주요 객체로 구성된다.

\begin{itemize}
\item \texttt{building} : 건물의 기본 정보
\item \texttt{constants} : 에너지 변환계수 및 비용계수
\item \texttt{site\_uses} : 용도별 연료 에너지 사용량
\item \texttt{source\_uses} : 1차 에너지 기준 사용량
\item \texttt{co2} : 온실가스 배출량
\item \texttt{cost} : 에너지 비용
\item \texttt{summary\_per\_area} : 면적당 성능 지표
\item \texttt{summary\_gross} : 건물 전체 기준 성능 지표
\end{itemize}

\subsection{class별 명세}

\subsubsection{건물 정보}

\texttt{building} 객체는 결과 계산에 사용된 바닥면적을 포함하여 건물의 기본 정보를 저장한다.

\subsubsection{변환계수 (constants)}

에너지 사용량을 1차 에너지, 온실가스 배출량, 그리고 비용으로 변환하기 위해 사용되는 계수들이 \texttt{constants} 객체에 정의된다.

\begin{verbatim}
constants
 ├─ site2source
 ├─ site2co2
 └─ site2cost
\end{verbatim}

각 항목은 연료 유형(전력:\texttt{electricity}, 천연가스:\texttt{natural\_gas}, LPG: \texttt{lpg},지역난방:\texttt{district\_heating}, 유류:\texttt{oil})별 변환계수를 정의한다.

\begin{itemize}
\item \texttt{site2source} : 에너지소요량[$kWh$]를 1차에너지소요량[$kWh$]로 변환하기 위한 계수
\item \texttt{site2co2} : 에너지소요량[$kWh$]을 온실가스 배출량[$kgCO\textsubscript{2,eq}$]으로 변환하기 위한 계수
\item \texttt{site2cost} : 에너지소요량[$kWh$]을 비용[$KRW$]으로 변환하기 위한 계수
\end{itemize}

\subsubsection{에너지소요량 (site\_uses [$kWh$])}

에너지소요량은 먼저 용도(end-use) 기준으로 분류되며, 각 용도는 다시 연료 유형별 월별 사용량 배열로 저장된다.

\begin{verbatim}
site_uses
 ├─ heating
     ├─ ELECTRICITY     : [12개월 배열]
     ├─ NATURALGAS      : [12개월 배열]
     ├─ LPG             : [12개월 배열]
     ├─ OIL             : [12개월 배열]
     └─ DISTRICTHEATING : [12개월 배열]
       (다른 end-use도 동일)
 ├─ cooling
 ├─ lighting
 ├─ circulation
 ├─ hotwater
 └─ generators
\end{verbatim}

\subsubsection{1차에너지소요량 (source\_uses [$kWh$])}
구조는 에너지소요량과 같다.

\subsubsection{온실가스 배출량 (co2 [$kgCO\textsubscript{2,eq}$])}
구조는 에너지소요량과 같다.

\subsubsection{에너지 비용 (cost [$KRW$])}
구조는 에너지소요량과 같다.

\subsubsection{집계: 면적당 (summary\_per\_area)}

각 항목별 합계를 건물의 바닥면적으로 나눈 값이다.

\begin{verbatim}
summary_per_area
 ├─ site_uses
     (연료별 집계)
     ├─ ELECTRICITY     
     ├─ NATURALGAS      
     ├─ LPG
     ├─ OIL             
     └─ DISTRICTHEATING 
     (end-use별 집계)
     ├─ heating     
     ├─ cooling      
     ├─ lighting
     ├─ circulation
     ├─ hotwater
     └─ generators      
     (합계)
     ├─ total_monthly : [12개월 배열]
     └─ total_annual
 ├─ source_uses
 ├─ co2
 └─ cost
\end{verbatim}

\subsubsection{집계: 총면적 (summary\_gross)}

구조는 면적당 집계와 같다.
